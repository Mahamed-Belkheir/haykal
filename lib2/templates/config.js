const packageJson = () => (``);

const tsConfig = () => (``);

const jestConfig = () => (``);

const ignore = () => (``);

module.exports = {
    packageJson,
    tsConfig,
    jestConfig,
    ignore,
}